const rows = [
{% for item in rowData %}
    {
    {% for key, value in item %}
      {{format(key)}}: {{ format(value) }} {{"," if not loop.last }}  
    {% endfor %}
    }{{"," if not loop.last }}  
{% endfor %}
];

export default rows;