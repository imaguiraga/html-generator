/*
const data = [{
  instances: '1',
  bar: 11,
  title: 'foo',
  unsortable: '14'
}, {
  instances: '3',
  bar: 7,
  title: 'sup',
  unsortable: '22'
}, {
  title: 'yee',
  instances: '2',
  bar: 8,
  unsortable: '1'
}];
//*/

const rows = [
{% for item in rowData %}
    {
    {% for key, value in item %}
      {{key}}: {{ format(value) }} {{"," if not loop.last }}  
    {% endfor %}
    }{{"," if not loop.last }}  
{% endfor %}
];

export default rows;